<!-- translation-source: docs/reports/suite-comparable-resources-2026-09-06.md; translation-source-sha256: a3fc42b705e9390b0635299bd2a579c66c242d74c8f1040eb102e71e5ade38aa -->

# 可比 Package 资源与保留的输入失败

与可比的优化前 Package `40101bb2` 相比，`e7b1170e` 的七种 Suite 工作负载均减少了 scope 内 CPU 用量。
各两个样本的 CPU 中位数下降 11.2–28.8%；每个候选值都低于对应的两个基线值。一个候选后台 Agent 样本
违反锁定输入门槛：命令补全在 48.868 ms 后出现，超过 40.465 ms 上限。后续通过样本不抵消这次失败。
整个 Suite 的验收仍未完成。

[数值记录](../../../../../docs/reports/suite-comparable-resources-2026-09-06.json)保留全部 32 个样本、单次门槛
违规、Source 身份及私有证据哈希。基线与候选使用相同的依赖锁文件和 Package 依赖声明。`40101bb2` 已包含
认证 Host 适配、Work Continuity 和 Goal 终态回复语义。[较早的生命周期对照](suite-lifecycle-comparison-2026-09-06.md)
使用更旧的基线，不能单独归因于此次优化。

## 成本与可见反馈

全部运行使用同一个 Pi 0.85.0 可执行文件
`0cfd1bf3e9468f1052d172502fa388e8e8e53dcdeb9fa97f1ef828fdd7757072`、内嵌 Bun 1.3.14 和 120×40 终端。
observer 使用 Bun 1.4.0。测量于 2026-09-06 17:42:14 至 17:57:55 UTC 串行执行，机器为 i9-13900H，
20 个在线逻辑 CPU，Linux `6.19.10-jc-xanmod1`。

批次按基线/候选/候选/基线顺序运行。每批依次执行 Goal、原生 Tool、Suite Tool、前台 Agent、后台 Agent、
Context、冷 Ledger Tool 和冷 Ledger 前台 Agent。每个样本新建私有配置及用户、网络、PID 命名空间。
没有并发运行本任务测试、子代理或其他 profiler；未控制机器上的其他活动、CPU 频率和内核页缓存。
没有丢弃预热样本或重试。每个版本的两个观测用于描述结果，不能保证总体分布。

| 工作负载 | CPU 中位数，秒：基线 → 候选 | 记账内存峰值中位数，十进制 MB：基线 → 候选 | 最长 Spinner 帧，ms：基线 → 候选 |
| --- | ---: | ---: | ---: |
| Goal 续行与终态回复 | 12.809 → 11.114 | 990.6 → 923.3 | 114.9 → 111.6 |
| 原生，两个 Bash Tool | 2.076 → 2.126 | 195.6 → 199.4 | 110.7 → 111.0 |
| Suite，两个 Bash Tool | 13.443 → 11.835 | 1,034.9 → 918.6 | 111.7 → 111.8 |
| 两个前台 Agent | 29.535 → 25.152 | 1,596.0 → 1,513.0 | 473.5 → 123.1 |
| 两个后台 Agent | 29.909 → 21.300 | 2,313.5 → 1,937.6 | 519.7 → 147.6 |
| 活跃 Context 写入/搜索 | 12.755 → 11.333 | 1,015.4 → 923.0 | 112.0 → 111.2 |
| 冷 Ledger，两个 Code Mode Bash Tool | 14.184 → 11.854 | 1,033.5 → 896.3 | 183.8 → 133.2 |
| 冷 Ledger，两个 Code Mode 前台 Agent | 30.920 → 25.809 | 1,592.6 → 1,530.8 | 572.3 → 135.4 |

CPU 和记账内存覆盖 Host cgroup，包括其中的 Worker 和已回收子进程，统计至退出前的最终观测。
observer、tmux、合成 HTTP 服务和 Ledger 种子创建不在该范围内；冷恢复包含在内。前台执行移至 Worker
并未把其 CPU 用量移出这些计数器。

并非所有内存指标都改善。直接前台 Agent 的最终存活进程 RSS 中位数从 645.4 增至 663.9 MB，冷 Ledger
前台场景从 684.8 增至 713.2 MB；其他 Suite 场景的最终 RSS 中位数下降。最终 RSS 是快照，不是进程树
合计峰值；记账内存峰值还包含其他费用，也不是累计分配量。最终进程 I/O 遗漏已退出子进程，不能证明总 I/O 节省。

每次 Suite 运行均自动请求并持久化一次 Naming，自动执行一次 Usage HTTP 刷新。Agent 场景完成两个子 Tool，
并按进程身份确认两个子进程均已回收；后台场景还在父 Agent 独立结束的情况下持久化两个不同的完成结果。
Context 发起三次原生请求并检索到写入证据；Goal 自动续行后持久化完成状态和唯一最终回复。
冷 Ledger 包含 24 次合成执行，每个结果为 800,000 字符；种子创建没有禁用校验。

## 为什么不能算通过

所有样本的观测连续：活跃 Spinner 无缺失，活跃覆盖至少 12.145 秒，最大采集间隙不超过 24.608 ms。
候选唯一超标是 `1-candidate-background`，证据 SHA-256 为
`d48fce849b085ef422a2360ccd5950faa97ae003f90121f202948d7f424141a7`。
命令补全准备始于 observer 时钟原点后 10,639.420 ms，之后三次采集仍无补全列表；列表与排队 Agent roster
于 10,688.288 ms 同时出现。这证明首次 Agent 启动附近的反馈延迟，不能定位负责函数。
没有将它作为观测缺口丢弃，也没有在缺少对应 Suite-off 复现的情况下归因于原生 Host。

两个基线前台、后台样本均违反 Spinner/输入门槛；两个冷 Ledger Tool 样本违反 Spinner 门槛，两个冷 Ledger
前台样本违反 Spinner/输入门槛。全部失败保留在记录中。[锁定门槛](../../../../../docs/reports/suite-responsiveness-gates-2026-09-05.json)
没有改变。候选 Spinner 最大值通过不能抵消独立输入失败，也不能解释此前保留的后段停顿。

较早的本地矩阵使用 `6d0507c1`，在运行候选之前就停于 Goal：该 Package 的终态 Tool 语义未产生当前夹具
要求的最终回复。其部分样本不计入本报告。两个独立的 `e7b1170e` CI 跟踪尝试也在基线工作负载阶段停止，
未比较候选（[第一次](https://github.com/jczhang02/pi-stuff/actions/runs/34048786759)、
[第二次](https://github.com/jczhang02/pi-stuff/actions/runs/34049432866)）。最后画面显示 Tool 已完成，Pi 仍处于
恢复状态；这些日志尚未定位原因。跟踪运行用于诊断，不是活性门槛验收。

分配/GC、进程树合计峰值 RSS、最长主线程工作区间及其余 owner/恢复场景仍需独立证据；下文的调度对照已补充唤醒计数。
本轮保留了表中工作负载的自动功能，但没有执行所有配置的外部服务，也没有完成[资源清单](suite-resource-inventory-2026-09-05.md)。

## 配置目录后续修复

在 `7fee89ed` 的后台 Agent CPU 诊断中，命令补全延迟区间采样到了经 `getProjectConfigDir` 调用的
`realpathSync`。该辅助函数每次调用都从可执行文件和 package 元数据重新查找 Pi 不变的配置目录名，遍历
每个项目上级目录时也会重复。现在直接使用其 SDK 导入中已公开的 Host 常量。真实发现回归测试针对相同的
嵌套/根目录查找，从四次同步 realpath 调用、32 次元数据读取尝试降至零。现有路径优先级、选中 Skills 和
MCP 检查通过。工具文件由 510 行减至 441 行，没有新增缓存或依赖。

四次未启用 profiler 的后台 Agent 运行，按基线/候选/候选/基线顺序比较干净的 `7fee89ed` 与这项单文件
Package 修改。它们复用上述固定可执行文件、命名空间、终端、新建配置、资源 scope 和锁定门槛。
每次均完成并回收两个子进程，记录两个后台结果，并自动执行 Naming/Usage 各一次。首次准备缺少新基线
worktree 的依赖，在 Session 启动前失败，没有产生工作负载样本。安装锁定依赖后，四次批次完整执行，没有丢弃样本。

| 样本 | CPU，秒 | 最终 RSS，十进制 MB | 记账内存峰值，十进制 MB | 最长 Spinner，ms | 最慢输入/准备，ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 基线 1 | 20.995 | 587.5 | 1,950.5 | 160.455 | 37.473 |
| 候选 1 | 21.267 | 597.3 | 1,959.3 | 123.937 | 72.710 |
| 候选 2 | 21.255 | 595.5 | 1,916.8 | 111.618 | 14.059 |
| 基线 2 | 21.237 | 589.1 | 1,938.2 | 114.089 | 14.183 |

这证明重复操作已被删除，不能证明整个负载节省资源或卡顿已修复。候选 1 仍违反 40.465 ms 输入门槛，
证据哈希为 `72fa8cb1d855b1cec7e9b7315a523641f0e59cc3629175d363ea766deb397e5f`。
命令补全准备始于 10,683.870 ms，在 10,756.580 ms 与排队 Agent roster 一同出现。全部采集间隙低于
26.230 ms，活跃 Spinner 观测无缺失。此前 profiler 的调用栈不能归因这次未采样失败，也不能解释最初的
48.868 ms 事件。数值记录保留全部四个结果及 Source/diff 哈希。

## 内核跟踪 CI 的时限失败

[运行 34051108002](https://github.com/jczhang02/pi-stuff/actions/runs/34051108002)保留的 Provider 和 Session
记录解释了基线前台观测为何不完整：两个子进程均成功结束，父进程在 2026-09-06 18:18:56.993 UTC 请求最终
回复，夹具安排四秒后发送。最后采集时间为 18:18:58.831 UTC，比计时器最早触发时间提前 2,161.883 ms。
记录中没有 Provider 错误。60 秒观测预算在被跟踪负载结束前用完；Tool 完成不能证明 Provider 死锁。
此前未保留这些记录的失败仍未归因。

调度诊断现使用独立的 75 秒观测预算，不能应用验收门槛，结果记录诊断用途和时限。普通 30/60 秒观测、
采集器外层 90 秒限制、隔离、完成检查及锁定响应性门槛不变。后续采集成功只能建立调度证据，不能抵消保留的输入失败。

## 已完成的调度对照

[运行 34052545498](https://github.com/jczhang02/pi-stuff/actions/runs/34052545498)在内核
`6.17.0-1022-azure` 上完成全部 28 个调度观测，按基线/候选/候选/基线顺序比较 Package `40101bb2` 与
`e51caab5`。全部样本丢失事件数为零，任务出生与退出记录完整。私有 trace 实例跟踪合成 Host 的线程、
Worker 及后代直至退出；observer、tmux、HTTP 夹具与 Ledger 种子创建不在范围内。无需更改本机权限。

| 工作负载 | `sched_wakeup` 中位数：基线 → 候选 | 变化 | `sched_wakeup_new` 中位数：基线 → 候选 |
| --- | ---: | ---: | ---: |
| 原生，两个 Bash Tool | 14,033 → 13,795.5 | −1.7% | 22 → 22 |
| Suite，两个 Bash Tool | 28,308 → 26,766.5 | −5.4% | 51 → 51 |
| 两个前台 Agent | 77,201 → 82,507 | +6.9% | 122 → 128 |
| 一个后台 Agent | 45,936 → 40,887 | −11.0% | 89 → 89 |
| Context 写入/搜索 | 24,096 → 23,637.5 | −1.9% | 25 → 25 |
| Goal 续行 | 23,730 → 23,318 | −1.7% | 25 → 25 |
| 冷 Ledger，两个 Code Mode Bash Tool | 28,730 → 27,274.5 | −5.1% | 61 → 60.5 |

这些样本中，前台隔离增加了唤醒成本。包括线程在内的任务数从 122 增至 128，两种实现均完成并回收相同的
两个子 Agent。比较隔离改动时，内部任务数不必相同，但全部成本仍计入结果。Ledger 基线两次均为 61 个任务，
候选为 60 和 61 个；摘要不能解释这一任务的差异。

此处后台场景只运行一个 Agent，不同于本地 CPU 批次中的两个。每个 Suite 观测保留自动 Naming/Usage；
前台、后台检查分别确认两个和一个子 Tool 完成。Context 进行三次投影并检索到证据，Goal 续行后完成。
数值记录的 `schedulerComparison` 保留产物哈希与全部单次计数。共享 runner 上每个版本两个观测不能建立
总体分布，也不能解释此前停顿的原因。跟踪时序不属于普通验收，唤醒也不是上下文切换。

## 后台输入的定点诊断

在 `e51caab5` 上分四次诊断输入失败，未改变生产行为。第一次 CPU profile 观测到 49.579 ms 反馈，
该区间 47 个样本中有 44 个位于原生布局渲染。采样也延长了其他普通反馈，因此这些栈不能解释两次未采样失败。

下一次直接计时记录 430 次绘制，累计 360.564 ms，最大 4.700 ms。另一轮对首次 FFI `flock` 加载的标记
测得 1.836 ms；两次均未复现反馈延迟。最后的文件系统调用跟踪记录父进程主线程 18,039 次已完成调用，
累计 198.967 ms，最大 8.947 ms，发生于启动时创建 Jiti 缓存文件。其最慢输入/补全准备为 14.240 ms。
这些只是对应运行的观测值，不能作为偶发渲染或 I/O 延迟的上界。

四个工作负载均完成并回收两个子 Agent、记录两个后台结果并保留自动 Naming/Usage。临时渲染包装、FFI
标记和 strace 命令均已撤除，数值记录绑定探针、profile 和观测的哈希。未依据这些未复现的探针重写渲染、
提前加载或修改文件系统操作。下一轮调查直接测量首次启动的加载区间。

## 后台冷加载跟进

在 `f6a3285e` 上进行十次轻量插桩，保留直接 dispatch 计时。最初四次测得第一次后台执行调用约为
147–160 ms，其中一次 62.654 ms 的选择反馈与其末段重叠。随后四次细分测得后台引擎加载为
46.709–55.369 ms。样本 `HKOgzn` 的补全准备在观察器相对时间 10,904.078 ms 开始，61.392 ms 后出现；
导入占据 10,898.152–10,944.861 ms，主线程 CPU 为 46.790 ms，调度等待为 0.048 ms。这确认了复现延迟期间
存在显著的 Suite 加载工作，但不能追溯证明每次此前未插桩失败的具体原因。

最后两次探针顺序显式加载原本已需加载的依赖：恢复读取器为 12.414/12.662 ms，runner-process 为
21.302/23.872 ms，后台模块其余部分为 12.855/13.136 ms。改变导入顺序的样本属于诊断，不是验收。
主线程 CPU 来自 `/proc` 调度计数，其更新粒度可能使短区间计数比经过时间多约一毫秒；包含 await 的区间
也不证明是单个连续 JavaScript 任务。十个样本都完成并回收两个子进程，保留两个后台结果和各一次自动
Naming/Usage；数值记录绑定全部探针与证据哈希。

本次改动将已完成构建的恢复记录发布与保留输入的解析分开。前台、后台启动共用同一个小型 writer，resume
仍使用原有读取器并完整校验。后台启动准备之后，必要的 runner-process 加载使用现有首次加载 timer 边界。
spawn 前导入失败时清理本次拥有且尚未启动的目录。仅供测试使用的 runner 控制重导出不再把下一阶段带入
启动模块。没有新增执行策略、刷新频率、缓存、依赖或权限。五个生产文件和两个测试从 1,761 行减至 1,758 行，
其中包含新增的 18 行恢复 writer。

四次未插桩运行按 ABBA 顺序比较干净的 `f6a3285e` 与保留完整改动的候选，使用相同的固定 Host、功能、
命名空间、资源 scope、终端和冻结门槛。没有并发的任务测试、代理或 profiler。两个候选均通过全部门槛；
基线 1 复现了 73.002 ms 的补全延迟。

| 样本 | CPU，秒 | 最终 RSS，十进制 MB | 内存计费峰值，十进制 MB | 最长 Spinner，ms | 最慢输入/补全准备，ms | 最慢选择，ms |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 基线 1 | 21.464 | 600.3 | 1,922.9 | 147.954 | 73.002 | 14.036 |
| 候选 1 | 21.552 | 582.9 | 1,948.0 | 113.064 | 13.821 | 13.788 |
| 候选 2 | 21.503 | 588.8 | 1,939.8 | 124.125 | 13.962 | 14.933 |
| 基线 2 | 21.373 | 605.4 | 1,930.1 | 136.869 | 25.202 | 13.698 |

CPU 中位数上升 0.5%，内存计费峰值中位数上升 0.9%，最终 RSS 中位数从 602.8 降至 585.9 MB。
这些总量不能证明本次跟进降低了整个工作负载的资源成本；去除的恢复读取器工作另有测量。两次候选通过也
不能证明偶发停顿不再出现。最终 RSS 与计费峰值不等于分配量或进程树 RSS 峰值。四次运行都完成两个子进程
与后台结果，验证回收并保留自动 Naming/Usage。捕获间隔均小于 16.893 ms，未缺失 active Spinner。
原有 60 秒观测预算及全部门槛不变。

三条模块加载回归在改动前失败、改动后通过。加载、启动、恢复和真实 Host 控制检查共通过 56 项测试、
242 条断言，`check:fast` 通过。首次隔离控制测试未指定私有状态目录，三个 governor setup 用例因 `EACCES`
失败；指定新建的私有状态目录后，四个真实 Host 用例全部通过，无需改变权限。失败的 setup 不计作验收通过。

## 单独保留普通 CI 失败

完成调度比较的同一次 `34052545498` 随后未通过普通验收：299 个隔离测试文件通过，
`test/responsiveness-pty.test.ts` 失败。Goal 测试、Tool benchmark 与 Package 验证因此未执行。单后台 Agent
样本 `RRFa7j` 的观察器间隔为 40.316 ms，超过 40 ms 上限，因此结论不充分。另一个 238.574 ms Spinner
停留在该间隔开始之前已经结束，不能用观察器间隔解释。两者均保留记录，尚未将该停留归因到具体函数。
这份较早 CI 结果与当前本地比较都未完成整套 Suite 验收或剩余的资源及恢复矩阵。

## 恢复资源对照

下一批于 2026-09-06 20:04:35–20:41:27 UTC 完成 52 次验证器运行，覆盖 176 个原生 Host 生命周期。
每种工作负载按候选/基线/基线/候选顺序执行：Package `e43cc9e1` 对比 `40101bb2`，使用相同的当前验证器、
精确 Pi 二进制及全新私有配置。user/network/PID namespace 保持离线。没有重叠的 Host 工作负载或其他分析器；
机器环境活动与内核页缓存未受控。现有恢复断言保持不变。

外部读取器采集等待退出进程的 CPU、进程树 RSS 采样值及 I/O；原生 Host 输出自然 GC 诊断。
[GC 解释](../research/ps-yon-profiling-permissions-20260906.md) 定义可接受完整周期的分配量下界，并排除歧义或交错记录。
这些诊断自身有开销，不是普通响应性样本。每个变体两次观察仅提供描述性对照。

| 工作负载 | 每次原生 Host 数 | CPU 中位数，秒：基线 → 候选 | 最大进程树 RSS 采样值中位数，十进制 MB：基线 → 候选 | 已捕获 GC 分配下界中位数，十进制 MB：基线 → 候选 |
| --- | ---: | ---: | ---: | ---: |
| Work 监视器取消及五种完成/错误路径 | 1 | 13.705 → 11.887 | 942.0 → 944.1 | 1,304.5 → 620.4 |
| Goal 普通、Code Mode、重载、压缩、阻塞及重试 | 7 | 38.281 → 27.540 | 944.6 → 891.4 | 6,792.5 → 2,013.4 |
| Context 投影、持久化、恢复及隔离 | 8 | 33.826 → 21.298 | 827.9 → 709.7 | 7,609.4 → 2,125.5 |
| Context 输入及中断 | 2 | 13.133 → 9.825 | 889.9 → 827.3 | 3,075.0 → 1,763.0 |
| Tool 历史渲染及恢复 | 5 | 27.799 → 20.275 | 852.9 → 739.4 | 4,967.1 → 1,534.6 |
| Tool 分组生命周期、压缩、恢复及树 | 5 | 40.776 → 31.775 | 1,121.1 → 992.3 | 5,679.1 → 2,048.5 |
| RTK 执行及恢复 | 2 | 9.156 → 6.113 | 833.2 → 728.4 | 1,912.0 → 544.4 |
| MCP 设置、连接、Tool 及历史 Session | 3 | 14.136 → 9.307 | 846.4 → 820.6 | 3,006.7 → 858.3 |
| BTW 执行及保留历史 | 3 | 10.412 → 5.868 | 823.3 → 703.3 | 2,718.2 → 664.5 |
| Notification 生命周期 | 2 | 14.678 → 11.641 | 828.6 → 748.1 | 2,106.2 → 730.1 |
| Ponytail 模式及恢复 | 2 | 9.724 → 6.751 | 828.9 → 754.2 | 1,887.8 → 521.8 |
| Todo 依赖完成及冷重放 | 2 | 7.976 → 5.112 | 849.0 → 707.5 | 1,817.4 → 450.5 |
| Web 搜索/抓取、续取及冷重放 | 2 | 9.492 → 5.315 | 850.8 → 713.0 | 2,094.8 → 494.5 |

13 种工作负载的 CPU 中位数均下降。Work 的 RSS 采样峰值略升。GC 一列不是总分配字节数：各次捕获覆盖不同，
且不包含原生分配、尚未回收的尾部、缺失周期及独立子进程日志。读取器接受了这一批 18,740 个已观察 GC 开始中的
17,770 个周期。数值记录的 `recoveryResourceComparison` 保留每个 Host 的计数、未接受开始、暂停总和/最大值、
源码及原始证据哈希。不从较低的捕获值推导总分配量下降或 GC 全面改善。

RSS 是各进程顺序读取值之和，不是瞬时峰值，也不是该峰值的已证明下界。共保留 44,373 个样本和 630 次进程读取竞态；
最大扫描耗时 44.846 ms，包括已无存活驻留进程的扫描。末次及末一秒 RSS 中位数也予以保留，但不称为稳态平台。
CPU 累加顺序 Host 生命周期；RSS 一列取这些生命周期中最大的采样和，不累加各自的独立峰值。
Worker 与其 Host 共享进程 RSS，只计一次。

等待退出 CPU 包含内核记账的后代；外部读取器、tmux/Expect 及同级 HTTP fixture 不在此范围。父进程 I/O 最大值保留
可获得的已等待子进程记账，不再次累加子进程计数。不可用或尚未等待的后代 I/O 可能缺失。块操作不是字节，
上下文切换不是唤醒。Work 的输出块操作中位数几乎不变，Context 输入及 Notification 略升。
上面的配对调度证据仍是唤醒测量；本批诊断没有重跑它。

两个新增 fixture 执行真实原生 Tool 和规范 Session 重放。Todo 创建两项任务、持久化依赖、完成两者并重启同一 Session；
重放任务状态必须等于最终原生 Tool 结果。Web 搜索、抓取超过 50,000 字符的文档、读取精确 2,000 字符切片并重启同一
Session。切片须保持相同且不重新抓取，同时拒绝 localhost 抓取。隔离 namespace 内以合成 fetch 响应和 mount 私有
hosts/NSS 文件代替不可用的 DNS；生产 SSRF 检查保持启用。这些 fixture 不认证线上搜索服务或外部凭据。
功能专用验证器补充独立的完整自动 Naming/Usage 观察。

设置失败单独保留。最初八次 Work/Goal 运行因错误序列化 Bun usage 访问器而没有完整资源记录；显式读取字段修复了读取器。
第一次分组运行的终端比裁剪 fixture 允许的更宽。修正后功能通过，但 namespace 退出导致最后一份读取记录丢失；
测试程序现在等待自身的最终记录。两次 Web 尝试保留了不可用的系统 NSS resolver，在续取前失败；私有 NSS 配置修复了
fixture。这些不完整尝试均不计入 52 次对照，未削弱任何产品断言。

## Agent 进程树内存与主线程间隔边界

20:58:40–21:04:28 UTC 的另外八次运行采用相同的两个子 Agent 前台账本及后台工作负载、Package 版本、120×40 终端及
候选/基线/基线/候选顺序。现有 cgroup 读取器计入 Host、Worker 和后代；外部进程采样其 RSS 直至关闭。
私有首个 Extension 在内存记录 10 ms 定时器回调起点。两种探针均不修改生产源码，也均未使用验收门槛。
八次全部完成并回收两个子进程，自动 Naming 请求及持久化各一次，Usage 刷新一次；后台运行还保留两个完成结果，
同时父进程独立继续。

| 工作负载 | CPU 中位数，秒：基线 → 候选 | 最大进程树 RSS 采样值中位数，十进制 MB：基线 → 候选 | 最终存活 RSS 中位数，十进制 MB：基线 → 候选 | 已捕获父进程/Worker GC 分配下界中位数，十进制 MB：基线 → 候选 |
| --- | ---: | ---: | ---: | ---: |
| 冷账本，两个 Code Mode 前台 Agent | 35.358 → 29.859 | 1,631.9 → 1,621.4 | 667.1 → 718.0 | 4,070.3 → 3,055.5 |
| 两个后台 Agent | 33.934 → 24.522 | 2,519.0 → 2,148.1 | 639.0 → 588.4 | 3,542.6 → 2,593.3 |

前台隔离仍保留更多最终 RSS。其可接受 GC 暂停总和的中位数为 4,127.030 → 4,132.704 ms，捕获周期数增加；
更低的已捕获分配值不代表更少的回收活动。父进程返回读取字节中位数也从 270.1 升至 299.6 MB。这些计数包含已等待
子进程 I/O 和诊断日志，不声明精确源码文件读取量。后台返回读取字节中位数从 286.7 降至 257.5 MB。
这里子进程 GC 日志不完整，但其 CPU 和 RSS 采样仍在被测进程树内。

| 工作负载 | 最大 Extension 加载回调间隔，ms：基线 → 候选 | 最大活动 Agent 回调间隔，ms：基线 → 候选 | 最大结算回调间隔，ms：基线 → 候选 |
| --- | ---: | ---: | ---: |
| 冷账本前台 | 5,710.463 → 3,220.157 | 582.492 → 42.545 | 17.573 → 24.812 |
| 后台 | 5,874.372 → 3,641.249 | 542.673 → 41.691 | 24.183 → 31.689 |

回调间隔为完全位于其内的非让出主线程任务提供耗时上界。它还包含空闲、GC、调度等待和探针开销；不是精确 JavaScript
任务时长。覆盖始于探针求值，止于其关闭回调。更早的 Host 启动及更晚的清理不在范围内，阶段标签可能跨越生命周期事件。
启动加载单独报告，不从活动 Agent 最大值中隐去。`agentResourceComparison` 保留逐次最大值、精确间隔位置、
采样间隙、进程数、GC 覆盖及证据哈希。

RSS 采样器最大扫描耗时 44.237 ms，最大起点间隔 58.189 ms。它们限制内存分辨率，不是独立终端观察器的间隙；
后者最大间隙低于 28.473 ms，且没有缺失活动 Spinner。候选在这些诊断中的 Spinner/输入/选择最大值分别为
132.582/36.740/25.733 ms。不以此替代无插桩锁定门槛验收，也不解释所有历史停顿。

## 保留代码版本的普通 CI 已完成

[运行 34056256262](https://github.com/jczhang02/pi-stuff/actions/runs/34056256262) 在精确提交
`e43cc9e11d509d1c068c42f63faf52919f53263d` 上通过 Fast 和断网 Acceptance。后者完成 `test:ci`、Tool Activity
基准及打包后的固定 Pi 验证。未请求可选调度探针；其较早的 28 次证据对所测源码仍有效。此前失败 CI 与停帧记录保持不变。
这次成功确立该版本的 CI 结果，不提供追溯归因，也不代表整个 Suite 的验收已完成。

## 当前源码复验与历史失败

21:20:05–21:24:36 UTC，`e43cc9e1` 的六次无插桩 Agent 运行通过所有锁定单事件门槛。使用原来的 60 秒预算、
精确 Pi 二进制、私有全新配置/缓存、隔离 namespace 和 120×40 终端。顺序为直接前台、冷账本 Code Mode 前台、
后台，再反向执行。没有预热、被丢弃的工作负载样本、重叠测试或子代理、GC 日志、CPU 分析或回调标记。
仅运行既有外部终端及资源 scope 观察器。机器环境活动与内核页缓存未受控。

| 样本 | 最大 Spinner，ms | 最慢稳定期输入/补全设置，ms | 最慢选择，ms | 最大观察间隙，ms |
| --- | ---: | ---: | ---: | ---: |
| 直接前台 1 | 114.337 | 15.510 | 14.063 | 16.900 |
| 冷账本前台 1 | 124.291 | 14.020 | 15.638 | 16.869 |
| 后台 1 | 123.633 | 25.376 | 14.056 | 16.112 |
| 后台 2 | 136.609 | 25.441 | 14.020 | 16.096 |
| 冷账本前台 2 | 123.408 | 25.599 | 25.748 | 16.150 |
| 直接前台 2 | 113.060 | 14.636 | 25.899 | 15.899 |

门槛文件 SHA-256 仍为 `db75fe458f275724b9a030c59395d888c0421d0515c4c6cca01c3e02ad39d677`。
每次完成两个子 Tool，回收两个已识别出生身份的子进程，Naming 请求及持久化各一次，Usage 刷新一次。
两次后台均保留两个完成结果及父进程独立完成。没有缺失活动 Spinner 观察；关闭后所有 scope 均卸载、inactive、dead。
首个采集器在有效原生采集后停止，因为它预期整个 worktree diff 为空，却发现了已知报告改动。随后按精确文档白名单
验证同一份采集，再继续余下五次运行。数值记录保留六次观察和相同报告 diff 的哈希；未替换任何原生样本。

历史失败属于不同源码阶段。未归因的 270.053 ms `9LkXjG` 及 489.594 ms `U4SAr9` 停顿发生在
[前台执行隔离](suite-responsiveness-observer-2026-09-05.md#foreground-agent-execution-isolation-2026-09-06)之前。
旧父进程执行路径后来已迁入逐次 Worker；必要的父进程 UI 和 Session 提交保留。两份轨迹均不能追溯确认精确函数，
也均未重分类为 Host-only。它们作为早期源码失败保留，与配对边界对照和当前直接/Code Mode 检查并列。

Worker 采用后的 e51 CI 前台样本 `Hpk3n9` 必须与这些更早停顿区分。其最慢输入/补全设置为 46.587 ms，观察间隙
27.253 ms。按本地锁定门槛属于前台输入失败，尽管该 CI 场景仅断言功能完成。它不是后台探针的 46.790 ms CPU 区间。
独立的 238.574 ms CI 后台停顿以及 48.868/72.710 ms 本地输入失败也保持不变。后续后台冷加载改动有独立复现的加载
区间、基线失败及两个无插桩通过候选。上述六次当前运行在共享恢复发布改动后覆盖两个前台路径和后台；这是源码变更后的
验证，不是对 e51 的无解释重试。

有限的当前源码门槛检查已完成。它们支持所测执行路径，不声明所有历史延迟都已有因果解释，也不声明罕见停顿永不再现。
当前源码若出现新的可归因失败，将重新打开其所属路径。整个 worktree 检查、完整独立审查及已验证公开交付，与这份
带日期测量结果仍是不同事项。
